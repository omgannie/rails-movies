<nav class="navbar navbar-inverse">
	<div class="container-fluid">
	  <div class="navbar-header">
	    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
	      <span class="sr-only">Toggle navigation</span>
	      <span class="icon-bar"></span>
	      <span class="icon-bar"></span>
	      <span class="icon-bar"></span>
	    </button>
	    <a class="navbar-brand" id="dukeflix" href="/">Dukeflix</a>
	  </div>
	  <div id="navbar" class="navbar-collapse collapse">
	  	<% if authorized_user %>
	  		<% if authorized_user.trusted_reviewer %>
	  			<ul class="nav navbar-nav navbar-left">
	  				<li class="navbar-link"><%= link_to "Add Movie", new_movie_path %></li>
	  			</ul>
	  		<% end %>
	  	<% end %>
	    <ul class="nav navbar-nav navbar-right">
	    	 <li class="navbar-link"><%= link_to "Home", root_path %></li>
	    	 <% if @show_categories %>
		    	 <li class="dropdown navbar-link">
		    	 	<a class="dropdown-toggle" data-toggle="dropdown" href="#">Categories<span class="caret"></span></a>
		    	 	<ul class="dropdown-menu">
		    	 		<% categories.each do |category| %>
		    	 			<li class="<% category.genre %> genre-list-item"><%= link_to category.genre, genre_path(id: category.id)  %></li>
		    	 		<% end %>
		    	 	</ul>
		    	 </li>
		    <% end %>
	      <% if logged_in? %>
	        <li class="navbar-link"><%= link_to "Logout", logout_path, method: 'delete' %></li>
	      <% else %>
	      	<li class="navbar-link"><%= link_to "Login", new_login_path %></li>
			<li class="navbar-link"><%= link_to "Register", new_user_path %></li>
	      <% end %>
	    </ul>
	  </div>
	</div>
</nav>
